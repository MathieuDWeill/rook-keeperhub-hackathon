# Contributing

1. Create a branch from `main`.
2. Run `make setup` once.
3. Run `make quality` before committing.
4. Never commit `.env`, private keys, API keys, wallet exports or funded credentials.
5. Keep policy changes deterministic and add tests for approved and rejected paths.
